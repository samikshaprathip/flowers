# Bouquet Backend

## Setup

1. Copy `.env.example` to `.env` and update the MongoDB connection string.
2. Install dependencies.
3. Start the API.

```
npm install
npm run dev
```

The API runs on `http://localhost:3001`.
